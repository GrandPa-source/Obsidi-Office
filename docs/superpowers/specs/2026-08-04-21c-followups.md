# 21c smoke follow-ups — findings and rulings

Date: 2026-08-04
Branch: `org-sites`
Source: `OB_Testing/Smoke Tests/21c-org-sites-v0.1-desktop.md`, run by Paul against v0.1.49
Status: findings triaged; fixes NOT yet built. Build after the entity-notes plan completes (Paul's
ruling: one deploy carrying notes tabs + these fixes together, so one re-test covers both).

## G1 — re-triaged from FAIL

Paul recorded FAIL on "8 errors". The check's own criterion was *"a `TypeError: … is not a function`
would mean a method lost to an edit"*. **No such error is present.** Every red line originates in the
OnlyOffice engine, Chromium, or browser security policy — none in `obsidi-office` code, none
referencing entities, taxonomy or any v0.1 surface.

| Error | × | Source | Status |
|---|---|---|---|
| `Failed to register a ServiceWorker … 'app://obsidian.md' not supported` | 3 | Browser policy | Structural; one per editor iframe |
| `Cannot read properties of null (reading '$b')` @ `sdk-all-min.js:2019` | 2 | OnlyOffice engine | **Already logged** in the project file as a known non-fatal minor (chart init; render proceeds, edits persist) |
| `Automatic fallback to software WebGL has been deprecated` | 3 | Chromium | Environmental (GPU driver) |
| `Refused to set unsafe header "Accept-Charset"` | 3 | Browser policy | XHR proceeds |
| `Obsolete: 'chat' parameter … deprecated` | 3 | OnlyOffice | Deprecation notice, not an error |

All appear on the docx/pptx/xlsx paths that shipped and were iPad-verified in July, so they predate
`org-sites`. **Recommendation: re-grade G1 to PASS with a note that the console carries known
pre-existing engine noise.** Paul's call — not changed unilaterally.

## Defects to fix

1. **B2 — the Sites datalist stops suggesting after the first comma.** A single `<datalist>` matches
   against the whole input value, so once `Apotex Centre, ` is present nothing matches. Also
   reported: arrow/Tab selection not working in that field. **B4 passed in project view**, so the two
   surfaces build the field differently — establish why before fixing, do not assume one cause.
2. **F5 — a second editor created immediately after the first stalls it.** Console shows
   `removed docKey: doc-0c0361ec8c64` right after the pptx became ready, then a fresh `XlsxView`
   with a new docKey once reloaded. Consistent with the first editor being torn down underneath the
   user. **P21 core, not org-sites.**
3. **C5 — a site page open in a second tab does not refresh.** Matches the ledger minor:
   `openDocDetail` reuses `getLeavesOfType(...)[0]`, so a second leaf goes stale.
4. **C7 — Tab does not complete `[[Bay`** in the container-notes CM6 editor. Likely no Tab binding
   on the completion extension.
5. **D3 — "New organization" → "New Organization"**, main.js:6880 (the button label only; the modal
   title at main.js:11812 is a different string — confirm which Paul meant before changing both).

## Feature request (from F5's note)

**Related Documents should show the file type** — a small chip reading `xlsx` / `docx` / `pptx` /
`md` in place of the generic link icon. Style it like the existing chips.

## D6 — mis-specified, plus a ruling

The check asked for a refusal path guarding command-palette document creation. **No such command
exists.** Registered commands are: Open Document Browser, Create container note, Create
organization, the `_document.md` migration dry run, PDF PoC open, Open current docx/pptx/xlsx, Open
landing page. The guard has nothing to guard, so this is not a code failure.

**Paul's ruling (2026-08-04):** keep the genuine entry points — **Open Document Browser** and
**Create organization** (A1 of this very smoke test used the latter) — and remove or hide the
developer-facing ones: the migration dry run and the PDF PoC command. Do not pursue a
zero-commands outcome; it would delete the path the smoke test exercised.

## Bookkeeping

**F1–F4 have no recorded results.** The table cells were mangled when the long F5 note was entered —
the Expected column is truncated mid-sentence and the Result cells are gone. Those four regression
checks must be re-run or re-recorded before the sheet can be called complete.

## Ordering

Paul's ruling: finish the entity-notes plan first, then build these, then **one** deploy carrying
both so a single re-test covers the notes tabs and these fixes together.
